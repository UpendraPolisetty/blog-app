const BaseUrl = `https://api.realworld.io/api/`;
const  ArticlesUrl = BaseUrl + "articles";
const TagsUrl = BaseUrl + "tags";
const localStorageKey = "TokenKey";

export {
    BaseUrl, ArticlesUrl, TagsUrl, localStorageKey
}